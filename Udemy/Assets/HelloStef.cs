using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloStef : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hello Stef!");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
